# 端口规则（必须遵守）

## ⚠️ 核心规则

**端口 8765 是 Omnia WebUI（FastAPI 版本）的专用端口。**

这是用户与 AI 沟通的唯一界面。用户通过浏览器访问 http://localhost:8765 来与 Omnia 交互。

## 禁止事项

1. 禁止建议用户重启 8765 端口的服务
2. 禁止建议用户杀掉 8765 端口的进程
3. 禁止在端口冲突时动 8765 端口

## 端口冲突处理

如果遇到端口 8765 被占用的问题：
- 应该检查是否有其他服务误用了 8765
- 应该让其他服务换端口，而不是动 8765
- 8765 端口必须始终保持运行
