# Omnia 工具调用问题修复报告

**修复时间**: 2026-04-29
**修复人员**: 无限
**问题**: Omnia 经常不调用工具，直接编造答案

---

## 🎯 修复总结

已完成 **5 项核心修复**，从根本上解决工具调用问题。

---

## ✅ 修复清单

### 修复 1：强化系统提示（wake.py）

**文件**: `src/omnia/wake.py`

**修改内容**:
- 添加正确/错误示例
- 添加判断标准
- 强化"硬规矩"指令

**修改前**:
```
### 🔴 硬规矩：必须先调用工具再回答
...（只有文字说明）
```

**修改后**:
```
### 🔴 硬规矩：必须先调用工具再回答
...（文字说明）

### ✅ 正确示例
**用户**: "检查一下 wake.py 的内容"
**你的行为**: 
1. 调用 read_file(path="/home/shan/omnia-os/src/omnia/wake.py")
2. 等待工具返回结果
3. 基于结果回答

### ❌ 错误示例
**用户**: "检查一下 wake.py 的内容"
**错误行为**: 直接回答"wake.py 是一个 Python 文件..."（未调用工具）

### 🔧 判断标准
**必须调用工具的场景**：
1. 需要读取文件内容 → read_file
2. 需要执行命令 → execute_shell
...
```

**效果**: LLM 现在有明确的示例参考，知道何时必须调用工具。

---

### 修复 2：改进 should_require_tool()（chat_handler.py）

**文件**: `src/omnia/chat_handler.py`

**修改内容**:
- 支持 provider 参数
- 对支持的 provider（kimi/openai/anthropic）返回 "required" 强制调用
- 对 DeepSeek 返回 "auto" 并依赖系统提示引导

**修改前**:
```python
def should_require_tool(user_message: str) -> str | None:
    """统一返回 "auto" 让模型自行决定。"""
    ...
    return "auto"
```

**修改后**:
```python
def should_require_tool(user_message: str, provider: str = "deepseek") -> str | None:
    """
    策略：
    - kimi/openai/anthropic: 支持 tool_choice: "required"，强制调用
    - deepseek: 不支持 "required"，返回 "auto" + 依赖系统提示引导
    """
    ...
    providers_support_required = ["kimi", "openai", "anthropic"]
    
    if provider.lower() in providers_support_required:
        return "required"  # 强制调用
    else:
        return "auto"  # 依赖系统提示引导
```

**效果**: 
- Kimi/OpenAI/Anthropic: 强制调用工具（100% 保证）
- DeepSeek: 依赖系统提示引导（已有强化）

---

### 修复 3：移除"工具执行后禁用工具"的逻辑（chat_handler.py）

**文件**: `src/omnia/chat_handler.py`

**修改内容**:
- 工具执行后仍然可以继续调用工具
- 通过 MAX_TOOL_ROUNDS 限制总轮数

**修改前**:
```python
use_tools = tools_schema if not tool_calls_executed else None  # ❌ 工具执行后禁用
```

**修改后**:
```python
use_tools = tools_schema  # ✅ 允许后续轮次继续调用工具
```

**效果**: LLM 可以在需要时连续调用多个工具，不再被强制停止。

---

### 修复 4：改进消息重建策略（chat_handler.py）

**文件**: `src/omnia/chat_handler.py`

**修改内容**:
- 允许继续调用工具
- 强调"不要编造，确保基于实际结果"

**修改前**:
```python
messages.append({
    "role": "user",
    "content": f"""基于之前的工具执行结果，请回答：{original_message}

记住：用自然语言回答，不要输出任何工具调用格式。"""  # ❌ 阻止工具调用
})
```

**修改后**:
```python
messages.append({
    "role": "user",
    "content": f"""基于之前的工具执行结果，请回答：{original_message}

注意：
1. 如果工具结果已足够回答问题，用自然语言回答即可
2. 如果还需要更多信息（如读取其他文件、执行其他命令），请继续调用工具
3. 不要编造或臆测，确保回答基于工具返回的实际结果"""
})
```

**效果**: LLM 可以在需要时继续调用工具，同时被明确要求"不要编造"。

---

### 修复 5：扩展 tool_preroll.py 的关键词覆盖

**文件**: `src/omnia/tool_preroll.py`

**修改内容**:
- 扩展第7条规则的关键词
- 添加第8条规则：特定文件检查
- 添加第9条规则：日志/错误检查

**修改前**:
```python
# 第7条规则关键词
r"(检查|确认|验证|核实|查一下|看看|检查一下|检测|测试|试一下|跑一下|分析|重新分析|完整分析|全面分析)"
```

**修改后**:
```python
# 第7条规则关键词 - 扩展
r"(检查|确认|验证|核实|查一下|看看|检查一下|检测|测试|试一下|跑一下|分析|重新分析|完整分析|全面分析|改好了吗|生效了吗|有没有生效|状态怎么样|怎么样了|完成了吗|成功了吗|好了没|有没有问题)"

# 新增第8条规则：特定文件检查
(r"(wake\.py|chat_handler\.py|web_server\.py|tool_preroll\.py|config\.py)", ...)

# 新增第9条规则：日志/错误检查
(r"(日志|log|error|错误|报错|异常|exception|traceback)", ...)
```

**效果**: 更多关键词被覆盖，前置工具检查能捕获更多场景。

---

## 📊 修复效果预期

| 场景 | 修复前 | 修复后 |
|------|--------|--------|
| 用户说"检查一下" | 可能不调用工具 | 100% 调用工具（kimi/openai/anthropic） |
| 用户说"改好了吗" | 可能不调用工具 | 前置检查 + 强制调用 |
| 工具执行后需要更多信息 | 被阻止，无法调用 | 允许继续调用 |
| LLM 尝试编造答案 | 无明确禁止 | 系统提示明确"不要编造" |
| DeepSeek provider | 只依赖系统提示 | 系统提示强化 + 前置检查 |

---

## 🧪 测试建议

修复后，建议测试以下场景：

1. **基础测试**:
   - "检查一下 wake.py 的内容"
   - "看看 Git 状态"
   - "有没有生效"

2. **连续工具调用测试**:
   - "检查 chat_handler.py，然后看看 Git 提交记录"
   - "读一下 config.py，然后检查端口状态"

3. **DeepSeek provider 测试**:
   - 切换到 DeepSeek，测试"检查一下"类消息
   - 验证系统提示是否有效引导

4. **Kimi/OpenAI provider 测试**:
   - 切换到 Kimi，测试"检查一下"类消息
   - 验证 tool_choice: "required" 是否生效

---

## 📝 后续优化建议

### 短期（本周）
1. **添加后验证机制** - 检查回答是否基于工具结果
2. **添加日志记录** - 记录工具调用情况，便于分析

### 中期（下周）
3. **添加 A/B 测试** - 对比修复前后的效果
4. **优化关键词列表** - 根据实际使用情况调整

### 长期（下月）
5. **添加机器学习模型** - 自动判断是否需要调用工具
6. **添加用户反馈机制** - 用户可以标记"应该调用工具但没调用"

---

## ✅ 验证结果

```bash
✅ 所有文件语法检查通过
✅ wake.py - agents_summary 强化完成
✅ chat_handler.py - should_require_tool() 支持强制调用
✅ chat_handler.py - 工具执行后不再禁用工具
✅ chat_handler.py - 消息重建策略允许继续调用工具
✅ tool_preroll.py - 扩展关键词覆盖
```

---

**修复完成！** 🎉

现在 Omnia 应该能够正确调用工具了。如果还有问题，请查看日志并反馈。
